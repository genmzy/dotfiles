# ============================================
# Ghostty Terminal - Complete Configuration
# ============================================
# View options: ghostty +show-config --default --docs

# includes
config-file = fonts/geist
config-file = theme
config-file = keybinds
config-file = appearance
config-file = behaviors
